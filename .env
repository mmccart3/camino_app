# Local credentials for developer tooling only. This file is ignored by Git.
# The current app needs no credentials and does not load this file.
# Never bundle secrets as Flutter assets or pass secrets through dart-define.
# Add KEY=value entries here only when a local tool actually needs them.
